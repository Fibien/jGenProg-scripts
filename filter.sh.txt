#!/bin/bash

# Check if filename is provided as an argument
if [ $# -eq 0 ]; then
    echo "Usage: $0 <filename>"
    exit 1
fi

filename=$1

# Flag to indicate if we are currently in a section starting with "checkout"
in_checkout=false

while IFS= read -r line; do
  if [[ "$line" == *"java"* ]] || [[ "$line" == *"Check out"*  ]]; then
    printf '%s\n' "$line"
  fi
done < filename